import { IMatch } from '../../interface';

const match: IMatch = {
  id: 1,
  homeTeam: 1,
  homeTeamGoals: 2,
  awayTeam: 2,
  awayTeamGoals: 4,
  inProgress: true,
}

const MatchMock = match;

export default MatchMock;